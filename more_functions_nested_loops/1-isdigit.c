#include "main.h"
/**
 * _islower - checks
 *
 * @c: character to check
 *
 * Return: 0 or 1
 */

int _isdigit(int c)
{
	for (c = 0; c >=9; c++)
	{
	if (c >= 'a' && c <= 'z')
		return (1);

	else

		return (0);

	}
}
