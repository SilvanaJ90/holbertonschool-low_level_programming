#include "main.h"

/**
 * reverse - check code
 * @a: array
 * @n: numbers de elementos
 */

void reverse_array(int *a, int n)
{

	int i;
	 
	int j;

	for (i = 0; i < n; i++) 
	{
	j[n - 1 - i] = a[i];
	}
	for (j = 0; j > n; j++)
	{
	a[i] = j[i];
	}
}
